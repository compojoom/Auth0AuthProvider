{
  "name": "auth0-realm",
  "description": "Package for the realm Server that authenticates the user using an JWT access_token obtained from auth0",
  "version": "0.0.1",
  "dependencies": {
    "jsonwebtoken": "^7.4.1",
    "jwks-rsa": "^1.2.0"
  }
}
